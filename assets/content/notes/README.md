# Notes Markdown Source

Put Markdown source files here if you want to keep editable `.md` files in the repo.

Convert one file manually:

```bash
python content_converter.py assets/content/notes/example.md --collection notes
```

Then copy the printed item into `NOTES` in `site_data.py`.
