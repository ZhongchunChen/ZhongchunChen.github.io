# Life Markdown Source

Put Markdown source files here if you want to keep editable `.md` files in the repo.

Convert one file manually:

```bash
python content_converter.py assets/content/life/example.md --collection life
```

Then copy the printed item into `LIFE_RECORDS` in `site_data.py`.
